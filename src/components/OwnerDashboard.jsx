import React from 'react';

const OwnerDashboard = ({ venue }) => {
  return (
    <div className="p-8 text-center text-gray-500 font-semibold">
      Partner Dashboard Coming Soon for {venue?.name}
    </div>
  );
};

export default OwnerDashboard;
